subprojects {
	repositories {
       mavenCentral()
	   mavenLocal()
    }
    apply plugin: 'eclipse'
	apply plugin: 'maven'

    version = '1.0-SNAPSHOT'
	group = 'com.dieend.adin'
}

task wrapper(type: Wrapper) {
    gradleVersion = '1.10'
}